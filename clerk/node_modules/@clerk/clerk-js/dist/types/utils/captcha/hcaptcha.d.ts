import type { CaptchaWidgetType } from '@clerk/types';
import type { CaptchaOptions } from './types';
export declare const getHCaptchaToken: (captchaOptions: CaptchaOptions) => Promise<{
    captchaToken: string;
    captchaWidgetType: CaptchaWidgetType;
}>;
